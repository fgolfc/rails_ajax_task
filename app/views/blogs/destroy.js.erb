$('.favorite-link').click(function(event) {
  event.preventDefault(); 
  var url = $(this).attr('href'); 
  $.post(url, { _method: 'delete' }, function() {
      $('#favorite_button, #unfavorite_button').toggle();
  });
});